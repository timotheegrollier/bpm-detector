# Minimal requirements for optimized build
# Full librosa is optional - falls back to numpy-only detection
numpy>=1.26
soundfile>=0.12.1

# Optional: Full librosa for better accuracy (adds ~50MB to bundle)
# Uncomment for full features:
# librosa>=0.10.2
